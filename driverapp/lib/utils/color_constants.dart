import 'package:flutter/material.dart';

class ColorConstants {
  // Primary brand colors
  static const primaryColor = Color(0xFF1976D2);  // Material Blue 700
  static const accentColor = Color(0xFF26A69A);   // Material Teal 400
  
  // Text colors
  static const textPrimary = Color(0xFF212121);   // Dark Grey
  static const textSecondary = Color(0xFF757575); // Medium Grey
  
  // Background colors
  static final scaffoldBackground = Colors.grey[50];  // Very Light Grey
  
  // Status colors
  static const successColor = Color(0xFF4CAF50);  // Green
  static const warningColor = Color(0xFFFFC107);  // Amber
  static const errorColor = Color(0xFFF44336);    // Red
  static const infoColor = Color(0xFF2196F3);     // Blue
  
  // Other common colors
  static final cardBackground = Colors.white;
  static final dividerColor = Colors.grey[300];

  static const Color assignedColor = Colors.orange;
  static const Color processingColor = Colors.blue;
  static const Color completedColor = Colors.green;
  static const Color profileColor = Colors.purple;
  
  static Color backgroundLight = Colors.blue.shade50;
}
